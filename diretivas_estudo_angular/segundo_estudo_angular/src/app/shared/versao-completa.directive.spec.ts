import { VersaoCompletaDirective } from './versao-completa.directive';

describe('VersaoCompletaDirective', () => {
  it('should create an instance', () => {
    const directive = new VersaoCompletaDirective();
    expect(directive).toBeTruthy();
  });
});
